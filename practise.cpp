/*#include <iostream>
using namespace std;

int main() {
    int n, i, j, space;

    cout << "Enter number of rows for the half-diamond (e.g., 5): ";
    cin >> n;

    // 1. Upper part of the diamond
    for (i = 1; i <= n; i++) {
        // Print leading spaces
        for (space = 1; space <= n - i; space++) {
            cout << " ";
        }
        // Print asterisks
        for (j = 1; j <= (2 * i - 1); j++) {
            cout << "*";
        }
        cout << endl;
    }

    // 2. Lower part of the diamond
    for (i = n - 1; i >= 1; i--) {
        // Print leading spaces
        for (space = 1; space <= n - i; space++) {
            cout << " ";
        }
        // Print asterisks
        for (j = 1; j <= (2 * i - 1); j++) {
            cout << "*";
        }
        cout << endl;
    }

    return 0;
}

*/
























